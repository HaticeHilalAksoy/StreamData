# StreamData
docker compose down -v --remove-orphans
docker-compose up --build ---> Yeni izinlerle entrypoint.sh'yi doğru şekilde mount eder, Container'ları yeniden oluşturur.
docker-compose up -d
docker compose ps
docker logs streamdata-webserver-1

docker-compose down
docker-compose up --build


source venv310/bin/activate
